FROM ghcr.io/astral-sh/uv:python3.12-bookworm-slim

WORKDIR /app

COPY agent-ui/pyproject.toml agent-ui/uv.lock ./
RUN uv sync --frozen --no-dev --no-install-project

COPY agent-ui/ .

RUN uv sync --frozen --no-dev

ENV PATH="/app/.venv/bin:$PATH"

EXPOSE 8501

CMD ["python", "-m", "app"]
